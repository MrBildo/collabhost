function DashboardPage() {
  return (
    <div>
      <h1 className="wm-section-title mb-6">Dashboard</h1>
      <p className="text-xs" style={{ color: 'var(--wm-text-dim)' }}>
        System overview with status strip, app table, and recent events.
      </p>
    </div>
  )
}

export { DashboardPage }

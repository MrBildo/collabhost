# Agent workspace (instance-local)
.agents/

# Claude Code (instance-local)
.claude/*

# Project instructions (instance-local) -- holds internal team/coordination
# detail; kept untracked so internals are never published to the public repo.
/CLAUDE.md

# Auth keys
.agents.env

# MCP config (user-level)
.mcp.json

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# .NET
backend/**/bin/
backend/**/obj/

# Aspire local state
.aspire/
aspire.config.json

# Collabhost app
backend/Collabhost.Api/wwwroot/
*.db
*.db-shm
*.db-wal
*.db.bak-*
backend/Collabhost.Api/db/
backend/Collabhost.Api/Data/.last-boot-version
backend/Collabhost.Api/Data/backups/

# IDE / editor
.vscode/
.vs/
.playwright-cli/

# Secrets / env
backend/**/Properties/launchSettings.json
!backend/Collabhost.AppHost/Properties/launchSettings.json
!backend/Collabhost.Api/Properties/launchSettings.json
backend/**/appsettings.Development.json
frontend/.env
frontend/.env.*

# Build info
frontend/tsconfig.tsbuildinfo
frontend/src/**/*.js

# Frontend test artifacts
frontend/junit.xml

# UAT fixture build output (recipes tracked under docs/uat-fixtures/recipes/, output is not)
docs/uat-fixtures/build/

# Playwright MCP
.playwright-mcp/

# Local tools -- ignore everything by default (downloaded binaries, scratch
# subprojects), but track the scripts and source files contributors share.
tools/*
!tools/build-caddy.ps1
!tools/build-caddy.sh
!tools/compute-wwwroot-hash.sh
!tools/generate-ids.cs
!tools/lint-source-comment-refs.py
!tools/lint-systemd-unit-drift.sh
!tools/test-source-comment-refs.sh

# OS files
Thumbs.db
Desktop.ini
.DS_Store
*.sarif
*.timestamp-*

{
  "slug": "system-service",
  "displayName": "System Service",
  "description": "Infrastructure process with no routing or port injection",
  "isInternal": true,
  "bindings": {
    "artifact": {
      "location": ""
    },
    "process": {
      "discoveryStrategy": "Manual",
      "shutdownTimeoutSeconds": 10
    },
    "restart": {
      "policy": "OnCrash"
    },
    "auto-start": {
      "enabled": true
    },
    "environment-defaults": {
      "variables": {},
      "schemaOverrides": {
        "variables": {
          "helpText": "Passed to Caddy's child process at startup. Use this for runtime secrets (e.g. CLOUDFLARE_API_TOKEN). Not the same as Caddy's {env.NAME} JSON-config substitution syntax -- that's configured in the proxy-config seam."
        }
      }
    }
  }
}
